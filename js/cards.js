export default class Card {
  constructor(button_name, title, description, text, article, date) {
    (this.button_name = button_name),
      (this.title = title),
      (this.description = description),
      (this.text = text),
      (this.article = article);
      (this.date = date);
  }
}
